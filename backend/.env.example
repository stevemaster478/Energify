# Copy this file to .env and fill in the appropriate values.

# Port for the Express server.  Defaults to 5000 if not specified.
PORT=5000

# MongoDB connection string.  If not provided, the API will run
# in stateless mode and skip persistence.  Example for a local
# deployment:
# MONGO_URI=mongodb://localhost:27017/energify
MONGO_URI=